function functionE() {
  const x =
    (parseInt(a.value) / parseInt(b.value)) *
    parseInt(c.value).value.toFixed(2);
  document.getElementById("info").innerHTML =
    "Сумма недостачи на сотрудника: " + x;
}
